//calculate area
function calculateplotarea(length,widith){
    //calculate the are of the plot by multiplyingthe length by widith
    let area = length * widith;
   // return the area
   return area;
}
// call function and out put
console.log(calculateplotarea(50,100));
